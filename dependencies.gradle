ext {
    // Build SDK version
    compileSdkVersion = 30
    targetSdkVersion = 30
    buildToolsVersion = "30.0.1"
    minSdkVersion = 21

    versionCode = 1
    libraryVersionName = '1.0.0-beta'

    // Bintray setting info
    repositoryName = 'library' // Bintray repository name
    packageName = 'com.herry.dimensize' // module name ex) compile

    description = 'Android Dimension Sizes'

    websiteUrl = 'https://github.com/HerryPark/AndroidDimensionSizes' // site url
    githubRepoUrl = 'https://github.com/HerryPark/AndroidDimensionSizes.git' // git repo url

    githubRepo = 'HerryPark/AndroidDimensionSizes'
    githubReleaseNotesFile = 'CHANGELOG.md'
}